# Property Maintenance Agent - Railway Deployment Configuration
# https://docs.railway.app/deployments/projects/config

[build]
builder = "NIXPACKS"
plan = "standard"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
